<%- include('header') %>

<section class="background-blue">
    <div class="container">
       <div class="row">
          <div class="col-md-6">
                <div class="titlepage">
                    <% if (user) { %> 
                        <h2>¡Bienvenido <%= user.name %>!</h2>
                    <% } %>
                   <br>
                   <p> Esta es tu página de perfil</p>
                </div>
          </div>
       </div>
    </div>

<%- include('footer') %>